#include <stdio.h>
#include <malloc.h>
struct ListNode
{
    int val;
    struct ListNode *next;
} ListLink;
typedef struct ListNode ListLink;

// 归并排序
ListLink *sortList(ListLink *head)
{
    
}
int main()
{
}
